datasource db {
  provider = "postgres"
  url      = env("DATABASE_URL")
}

// this is foo
generator foo {
  // it is a nexus
  provider = "nexus"
}

model Bar {
  id Int @id @first(fields: [one, two]) @second(fields: ["three", "four"])
}

enum Role {
  USER
  ADMIN
}
